(function () {

     API.chatLog("the script is down right now. we will have it back up soon");
                    }
                }
            }
        }
    };

    loadChat(trapbot.startup);
}).call(this);
